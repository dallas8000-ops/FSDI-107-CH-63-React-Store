# Optional: full URL to API when not using same-origin / reverse-proxy
# VITE_API_URL=http://localhost:3002

# Optional: override Vite’s `/api` proxy (defaults to http://localhost:3002 — same as PORT in server/.env)
# API_PROXY_TARGET=http://localhost:3002
